import { TestConfig } from "option_checker";

export const testConfig: TestConfig = {
        basic: {
                description: 'Basic',
                arg: 'abc',
                decl: { type: 'string' }
        },
};